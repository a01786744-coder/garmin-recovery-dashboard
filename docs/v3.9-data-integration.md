# v3.9 — Full Forerunner 970 data integration

Goal: render **everything already stored** and fetch+store+render **everything
fetchable**. Field names below are confirmed against the user's live account
(one-time probe, tokens deleted after).

## Zero extra API calls (parsed from payloads fetch_day already pulls)
| Field | Source | Column |
|---|---|---|
| Recovery time (min) | `get_training_readiness` → `recoveryTime` | daily `recovery_time_min` |
| Nap time (s) | `get_sleep_data` → `dailySleepDTO.napTimeSeconds` | daily `nap_time_s` |
| Skin temp deviation °C | `get_sleep_data` → `avgSkinTempDeviationC` | daily `skin_temp_dev_c` |

## +1 call/day each (today only, never backfilled)
| Field | Source | Column |
|---|---|---|
| SpO2 avg / lowest / sleep | `get_spo2_data` | daily `spo2_avg`, `spo2_lowest`, `spo2_avg_sleep` |
| Hydration ml / goal / sweat | `get_hydration_data` | daily `hydration_ml`, `hydration_goal_ml`, `sweat_loss_ml` |

## +1 call/day each (perf snapshot, today only)
| Field | Source | Column |
|---|---|---|
| Fitness age (WORKING source) | `get_fitnessage_data` → `fitnessAge` | perf `fitness_age` |
| Running tolerance load/ceiling | `get_running_tolerance` (latest wk) | perf `running_tolerance_load`, `running_tolerance_ceiling` |
| Hill score | `get_hill_score` → `maxScore` | perf `hill_score` |
| Lactate threshold HR / FTP | `get_lactate_threshold` | perf `lt_hr`, `lt_power` |
| Body weight (g) | `get_body_composition` → `totalAverage.weight` | perf `body_weight_g` |

## Per-activity (on-demand, cached) — +1 call per activity opened
| Field | Source | Column |
|---|---|---|
| Running dynamics + power | `get_activity` → `summaryDTO` (cadence, strideLength, groundContactTime, verticalOscillation, verticalRatio, averagePower, normalizedPower, maxPower, elevationGain) | activity_detail `dynamics_json` |

## Already stored, just needs UI
- `altitude_acclimation` (perf) → Trends tile
- `hrv_status` (daily) → badge on HRV card
- activity `weather` (cached) → conditions row in activity detail
- intraday `hr` (daily_intraday) → HR curve in detail panel
